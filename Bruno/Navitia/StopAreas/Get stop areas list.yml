info:
  name: Get stop areas list
  type: http
  seq: 1
  tags:
    - Stop Areas

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/stop_areas"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
