info:
  name: Get networks list
  type: http
  seq: 1
  tags:
    - Networks

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/networks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
