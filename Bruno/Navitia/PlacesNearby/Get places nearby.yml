info:
  name: Get places nearby
  type: http
  seq: 1
  tags:
    - Places

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/places_nearby"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
