info:
  name: Get stop schedules
  type: http
  seq: 1
  tags:
    - Stop Schedules

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/stop_points/{{stopPointId}}/stop_schedules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
