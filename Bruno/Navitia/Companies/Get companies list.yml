info:
  name: Get companies list
  type: http
  seq: 1
  tags:
    - Companies

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/companies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
