info:
  name: Get calculated journeys
  type: http
  seq: 1
  tags:
    - Journeys

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/journeys"
  params:
    - name: forbiddenUris[]
      value: "[]"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
