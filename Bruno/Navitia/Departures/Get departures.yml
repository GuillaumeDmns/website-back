info:
  name: Get departures for a stop point
  type: http
  seq: 1
  tags:
    - Departures

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/stop_points/{{stopPointId}}/departures"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
