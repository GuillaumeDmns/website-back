info:
  name: Get lines list
  type: http
  seq: 1
  tags:
    - Lines

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/lines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
