info:
  name: Get commercial mode by ID
  type: http
  seq: 1
  tags:
    - Commercial Modes

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/commercial_modes/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
