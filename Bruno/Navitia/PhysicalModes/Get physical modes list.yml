info:
  name: Get physical modes list
  type: http
  seq: 1
  tags:
    - Physical Modes

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/physical_modes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
