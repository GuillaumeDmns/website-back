info:
  name: Get traffic reports
  type: http
  seq: 1
  tags:
    - Traffic Reports

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/traffic_reports"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
