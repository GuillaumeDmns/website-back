info:
  name: Get routes list
  type: http
  seq: 1
  tags:
    - Routes

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/routes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
