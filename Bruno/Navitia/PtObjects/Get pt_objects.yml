info:
  name: Get public transport objects
  type: http
  seq: 1
  tags:
    - PT Objects

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/pt_objects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
