info:
  name: Get route schedules
  type: http
  seq: 1
  tags:
    - Route Schedules

http:
  method: GET
  url: "{{baseUrl}}/api/navitia/routes/{{routeId}}/route_schedules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
